apiVersion: v1
data:
  config.json: |-
    {
      "sink": "glog",
      "enable-prometheus": "true"
    }
kind: ConfigMap
metadata:
  labels:
    app: eventrouter
    chart: eventrouter-0.4.0
    heritage: Helm
    release: RELEASE-NAME
  name: RELEASE-NAME-eventrouter
  namespace: default
